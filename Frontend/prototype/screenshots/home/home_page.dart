import 'package:flutter/material.dart';
import '../../../lib/features/home/presentation/pages/home_page.dart' as real;

// Esta é a versão limpa e funcional da HomePage.
// O código real e organizado está em lib/features/home/presentation/pages/home_page.dart
// Mas mantemos esta versão aqui para facilitar seu protótipo.

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const real.HomePage();
  }
}